<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.root.login">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.HomePage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />

        <activity android:name=".LogInActivity" />
        <activity android:name=".activity.Details"
            android:label="" />
        <activity android:name=".activity.Cart" />
        <activity android:name=".activity.Section1" />
        <activity
            android:name=".UploadProduct"
            android:label="@string/title_activity_upload_product"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>


</manifest>